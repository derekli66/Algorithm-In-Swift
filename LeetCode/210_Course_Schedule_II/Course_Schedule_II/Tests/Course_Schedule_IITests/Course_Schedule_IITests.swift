import XCTest
@testable import Course_Schedule_II

final class Course_Schedule_IITests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(Course_Schedule_II().text, "Hello, World!")
    }
}
